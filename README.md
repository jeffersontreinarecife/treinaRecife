## 🛠️ Comandos Básicos do Git

Aqui estão os comandos principais que utilizo neste projeto:

### 📥 Início e Atualização
* `git clone <url-do-repositorio>`: Clona o projeto para sua máquina.
* `git pull origin <nome-da-branch>`: Atualiza seu repositório local com as últimas mudanças do servidor.

### ✍️ Salvando Alterações
* `git status`: Verifica quais arquivos foram modificados.
* `git add .`: Adiciona todos os arquivos alterados para a fila de envio.
* `git commit -m "Sua mensagem aqui"`: Cria um ponto de salvamento com uma descrição.
* `git push origin <nome-da-branch>`: Envia suas alterações para o GitHub.

### 🌿 Trabalhando com Branches
* `git checkout -b <nome-da-nova-branch>`: Cria e entra em uma nova ramificação.
* `git checkout <nome-da-branch>`: Troca para uma branch existente.
* `git branch`: Lista todas as branches do projeto.
* `git merge <nome-da-branch>`: Une as alterações de outra branch na que você está agora.

---
